# Ferramentas

DETRAN MS: Veículo expõe 1 ferramenta (todas somente leitura).

### 1. `detran_ms_veiculo_consultar`
**Input**: `login_cpf` (opcional), `login_senha` (opcional), `pkcs12_cert` (opcional), `pkcs12_pass` (opcional), `placa`, `renavam`

DETRAN MS: Veículo, consulta em fonte oficial.

## Prompts de exemplo

```
Consultar DETRAN MS: Veículo
```
